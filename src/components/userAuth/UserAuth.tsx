import React, { VFC } from 'react';
import AuthButton from './AuthButton';

const UserAuth: VFC = () => {
  return (
    <div>
      <h4>user auth</h4>
      <AuthButton />
    </div>
  );
};

export default UserAuth;
